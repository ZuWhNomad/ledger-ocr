# Core (required)
pdfplumber>=0.11        # born-digital text/table extraction; also rasterizes via pypdfium2 (Apache) for OCR
openpyxl>=3.1           # XLSX export (CSV export uses the stdlib, no dep)

# Optional
pytesseract>=0.3.10     # OCR fallback for scans/images -- ALSO needs the Tesseract binary (see README)
requests>=2.31          # talk to a local Ollama for optional LLM error-checking

# Dev / sample generation only
reportlab>=4.0          # generates the synthetic sample PDF in samples/make_sample.py

# Notes:
# - Pillow and pypdfium2 come in as pdfplumber dependencies.
# - No PyMuPDF (AGPL) / camelot (Ghostscript) / tabula (Java): avoided on purpose (see PLAN.md).
