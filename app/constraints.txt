# Pinned versions for a reproducible, tamper-evident from-source install (SEC-P3-3).
# These are the exact versions this project is developed and tested against; the
# frozen installer bundles the same set. Install with:
#
#     pip install -r requirements.txt -c constraints.txt
#
# Direct dependencies
pdfplumber==0.11.10
openpyxl==3.1.5
pytesseract==0.3.13
requests==2.34.2
reportlab==4.5.1
python-docx==1.2.0
# Transitive dependencies pinned so the resolver can't silently pull a newer
# (or compromised) release of anything that runs at import/OCR/PDF time.
pdfminer.six==20260107
pypdfium2==5.11.0
pillow==12.2.0
et_xmlfile==2.0.0
charset-normalizer==3.4.7
certifi==2026.7.22
idna==3.19
urllib3==2.7.0
lxml==6.0.2
